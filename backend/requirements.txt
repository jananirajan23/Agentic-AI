fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
groq>=0.11.0
PyMuPDF>=1.24.0
python-docx>=1.1.0
requests>=2.31.0
langgraph>=0.2.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# --- Auth + dashboard persistence ---
SQLAlchemy>=2.0.0
PyJWT>=2.8.0

# --- Architecture Structure Agent: OCR extraction ---
# Pure-Python OCR (onnxruntime-backed) — no separate Tesseract system
# binary required, unlike pytesseract.
rapidocr-onnxruntime>=1.2.0

# --- Architecture-Modules Alignment Agent: embeddings ---
# Pulls in torch (CPU) as a transitive dependency; the first run downloads
# and caches the ~90MB all-MiniLM-L6-v2 model from Hugging Face.
sentence-transformers>=3.0.0
numpy>=1.24.0

# --- Testing ---
pytest>=8.0.0
pytest-mock>=3.12.0
httpx>=0.27.0
